export type Stat = { value: string; label: string };

// Mirrors the real tenure in lib/data/experience.ts (Feb 2025 — present).
export const heroStat: Stat = { value: "1.5 yr", label: "Shipping in production" };
